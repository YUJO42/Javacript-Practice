const dummy = [];
for (let i = 0; i < 100; i++) {
  dummy.push({ data: i });
}

const tableTemplate = () => {
  return ``;
};

export default function page() {
  const $pageContainer = document.querySelector('#page-container');
}

new page();
